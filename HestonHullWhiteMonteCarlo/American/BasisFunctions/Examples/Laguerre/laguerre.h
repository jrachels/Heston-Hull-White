
#include <cmath>

//double a = std::laguerre(1, 2);

namespace heston_hull_white {
	namespace monte_carlo {
		namespace examples {

			// LAGUERRE MUST RETURN A VECTOR OF RESULTS
			// THEREFORE IT SHOULD JUST TAKE THE MATRIX AND MODIFY IT
			// 
			// WARNING, NO BOUNDS CHECKING

		
			void laguerre(int n, double x, iterator start, iterator end) {
				// I either don't need end or I don't need n.

			}

			//std::laguerre
		}
	}
}